// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  // output   = "../src/generated/prisma"
  
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}



model AnomaloJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  department  String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}


model CredoJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  location    String
  department  String
  workplaceType String?
  employmentType String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LaurelJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  department  String
  location    String
  employmentType String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PivotalJob {
  id          String   @id @default(uuid())
  title       String  
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ChecklyJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  department  String
  location    String
  workplaceType String?
  employmentType String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  checklyLocation ChecklyLocation[]
}

model ChecklyLocation {
  id          String   @id @default(uuid())
  locationId  String   
  locationName String
  checklyJob  ChecklyJob @relation(fields: [checklyJobId], references: [id], onDelete: Cascade)
  checklyJobId String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VoizeJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  department  String
  location    String
  workplaceType String?
  employmentType String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  voizeLocation VoizeLocation[]
}

model VoizeLocation {
  id          String   @id @default(uuid())
  locationId  String
  locationName String
  voizeJob    VoizeJob @relation(fields: [voizeJobId], references: [id], onDelete: Cascade)
  voizeJobId  String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FarSightJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  locations   FarSightLocation[]
}

model FarSightLocation {
  id          String   @id @default(uuid())
  locationId  String
  locationType String
  locationName String
  farSightJob  FarSightJob @relation(fields: [farSightJobId], references: [id], onDelete: Cascade)
  farSightJobId String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TeleportJob {
  id          String   @id @default(uuid())
  title       String
  teamName    String
  location    String
  type        String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SafeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  group String
  department  String
  workplaceType String
  employmentType String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MaraTalentJob {
  id          String   @id @default(uuid())
  title       String
  company    String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MaterializeJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LegionJob {
  id          String   @id @default(uuid())
  title       String  @unique
  location    String
  type        String
  compensation String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LoopJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String
  department  String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EnterpretJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SeeChangeJob {
  id          String   @id @default(uuid())
  jobId       String @unique
  title       String
  department  String
  employmentType String
  city        String
  state      String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Lepton {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RecruitmentJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String
  location    String
  salary      String
  href        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OperantaiJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WebaiJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  department  String
  location    String
  employmentType String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CardinalopsJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  meta        String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DoxelJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  department  String
  workplaceType String
  employmentType String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RoboFlowJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  department  String?
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DashJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  employmentType String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LumosJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FormantJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FluidStackJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  department  String
  location    String
  workplaceType String?
  employmentType String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  fluidStackLocation FluidStackLocation[]
}

model FluidStackLocation {
  id          String   @id @default(uuid())
  locationId  String
  locationName String
  fluidStackJob    FluidStackJob @relation(fields: [fluidStackJobId], references: [id], onDelete: Cascade)
  fluidStackJobId  String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AbnormalJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VorlonJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([title, department, location])
}

model OmneaJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  department  String
  location    String
  employmentType String
  compensation  String?
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SysdigJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  group       String
  department  String
  workplaceType String
  employmentType String?
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PortJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EndorLabsJob {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model IoJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String
  location    String
  department  String
  employmentType String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}


model HightouchJob {
  id          String   @id @default(uuid())
  title       String  
  location    String
  department  String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RelyanceJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String
  department  String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AntithesisJob {
  id          String   @id @default(uuid())
  jobId      String @unique
  title       String  
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model GetbalanceJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BasetenJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BlockaidJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BlinkOpsJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BynderJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CastAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ChainguardJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CheckmarxJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CockroachLabsJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CognitionJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MerlinJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DeepsetJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FactoryJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ForterJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model GroundcoverJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HaiiloJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model HebbiaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HoneycombJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model IslandJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LaunchDarklyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MablJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MazeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MergeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MetalBearJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MindtickleJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model NavanJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OasisSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OctopusJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OpalJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OrcaAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RedwoodJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SingleStoreJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SnorkelAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model UserTestingJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VegaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VezaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WatershedJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WorkOSJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model IncidentJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PlaxidityxJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ScytaleJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model TraceBitJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CynomiJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model KurrentJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model YoobicJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model StackOneJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SevenAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OpaqueJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model ClickHouseJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CoderJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ContextualJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CorridorJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DecagonJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DreamJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ElevenlabsJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FoundationalJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Horizon3Job {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model InstruqtJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PhoebeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model StainlessJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TurnkeyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WriterJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AvocaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CertikJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PushSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SpruceJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TwingateJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LayerzeroJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LightrunJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LinearJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MedallionJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model NomaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PactumJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model NumericJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PenteraJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PlayzeroJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RekaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ReplicateJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ResolveJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SeonJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SgnlJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SierraJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SolaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model StacklokJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AponoJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ArmadaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BrowserbaseJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CasapJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CyfirmaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DataikuJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EthycaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FiddlerJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FlockSafetyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FyxerJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DbtlabsJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model GraviteeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HyprJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model KognitosJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LakeraJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LangChainJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LogicGateJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MinJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ObsidianSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PigmentJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ProphetSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ScylladbJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SilverFortJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TavilyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TruliooJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model UnframeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model UpwindJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WorkatoJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZafranJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZipsecJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZenityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZeroNetworksJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model AleoJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AsimilyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BolsterJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EderaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SecureframeJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PayabliJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model UnstructuredJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RhombusJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CogentJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DeepgramJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FernJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FieldGuideJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ProfoundJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SymbioticJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TalosJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ApolloGraphQlJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FlutterFlowJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model InfisicalJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ReflectionaiJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ClearVectorJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FableJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ModalJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LovableJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ExaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model KongJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OpteryJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WorldJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DustJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DaylightJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DuneJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LegitJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model XMCyberJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CynerioJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AkeylessJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FiniteStateJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HalcyonJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OrkesJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MindsdbJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OrcaSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AirtableJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model KindoJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model GalileoJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CerbyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String 
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model SecuritiJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ChalkJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DatavisorJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model TatumJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model TrustmiJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CriblJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OmniJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RunrevealJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TokenSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SuperAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OpsinJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ExaforceJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SynadiaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CoderabbitJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OscilarJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PrimeSecurityJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LinxJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VClusterJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VercelJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model VelotixJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SpectrocloudJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RoxJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ConductJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SynthflowJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EndeavorJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ChronosphereJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WordsmithJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MoonvalleyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SolaAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AmebaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CluelyJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SalientJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AnteriorJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CredalJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model JuliusJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CampfireJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PeecJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RetellJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PulseJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model NumeralJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model WordwareJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model LlamaindexJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model NormAIJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PylonJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BasisJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TabsJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DideroJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AssorthealthJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model GreenliteJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FurtherJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EloquentJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TraversalJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AlbertinventJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([location, href])
}

model StarcloudJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZockJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model DualEntryJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZestJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PayflowsJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HarperJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HadriusJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model CrewaiJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model QuantaJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SapienJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model OsiumJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PlanhatJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ZiplineRetailJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BluefishJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model RunzeroJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model EchosoftwareJob {
  id          String   @id @default(uuid())
  title       String
  location    String
  href        String  @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}